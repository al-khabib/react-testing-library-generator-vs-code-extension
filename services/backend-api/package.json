{
  "name": "backend-api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for VS Code extension",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fastify/cors": "^8.4.2",
    "@fastify/jwt": "^8.0.1",
    "@fastify/oauth2": "^7.8.0",
    "dotenv": "^17.2.2",
    "fastify": "^4.25.0",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.6.2",
    "typescript": "^5.3.3"
  }
}
